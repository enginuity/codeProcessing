This contains examples that probably only work on my computer (since they depend on my file structure / projects). 

Might move this folder from the main branch, and instead have an 'examples' folder or something. (maybe can use examples in documentation instead?)